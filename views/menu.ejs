<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Shout </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse"
    data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav d-flex">
      <% if (locals.user) {  %>
      <a class="nav-link p-2" href="/">Home</a>
      <a class="nav-link p-2" href="/post">New post</a>
      <% } else { %>
      <a class="nav-link p-2" href="/">Home</a>
      <% } %>
    </div>
    <div class="navbar-nav d-flex ml-auto">
      <% if (locals.user) {  %>
      <a class="nav-link ml-auto text-secondary p-2" href="/logout">Logout</a>
      <% } else { %>
      <a class="nav-link ml-auto text-dark btn btn-info" href="/login">Log In</a>
      <% } %>
    </div>
  </div>
</nav>
